"""Infrastructure layer.

Concrete adapters for the application's ports: persistence, configuration,
logging and (from P2) model providers. Depends on the application and domain
layers; nothing in those layers depends on this one.
"""
