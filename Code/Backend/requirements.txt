# NanoVox backend — runtime dependencies.
# Dev/test/lint tooling lives in requirements-dev.txt.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
SQLAlchemy[asyncio]>=2.0.36,<2.1
aiosqlite>=0.20,<1.0
# Postgres driver — a deployment (e.g. a free-tier Neon database) uses this
# instead of aiosqlite, so its storage survives a redeploy. Local/default dev
# stays on SQLite; DATABASE_URL picks which one is actually in use.
asyncpg>=0.29,<1.0
alembic>=1.14,<2.0
PyYAML>=6.0,<7.0
# Multipart form parsing, which FastAPI needs to accept an uploaded file.
python-multipart>=0.0.9,<1.0
# Reading the call-corpus PDF. A runtime dependency since the corpus can be
# imported from the Corpus import screen, not only by the offline script.
pypdf>=5.1,<7.0

# Model providers. Official SDKs rather than hand-rolled HTTP: they track API
# changes, expose typed errors, and are the supported path for structured output.
anthropic>=1.0,<2.0
openai>=1.60,<3.0
# Ollama speaks plain HTTP; httpx2 is already the transport anthropic 1.x uses.
httpx2>=2.12,<3.0
